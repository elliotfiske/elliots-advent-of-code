function part1(input: Array<string>) {
  const ints = input.map((i) => parseInt(i));

  let result = 0;
  ints.forEach((i) => {
    result += Math.floor(i / 3) - 2;
  });

  return result;
}

function part2(input: Array<string>) {
  const ints = input.map((i) => parseInt(i));

  let result = 0;
  ints.forEach((i) => {
    let
    while ()
    result += Math.floor(i / 3) - 2;
  });

  return result;
}

export { part1, part2 };
